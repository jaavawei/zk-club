spring:
  application:
    name: zk-club-subject-dev
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 196.168.11.75:8848
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        namespace:
        file-extension: yaml
      discovery:
        enabled: true
        server-addr: 196.168.11.75:8848